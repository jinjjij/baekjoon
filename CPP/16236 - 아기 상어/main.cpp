#include <iostream>
#include <vector>


int N;
std::vector<std::vector<int>> input;


int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);

    

    return 0;
}