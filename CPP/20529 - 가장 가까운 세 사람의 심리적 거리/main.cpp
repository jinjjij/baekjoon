#include <iostream>


int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);

    int T = 0;
    std::cin >> T;

    int N = 0;
    bool mbti[4];
    for(int t=0;t<T;t++){
        std::cin >> N;
        for(int i=0;i<N;i++){
            std::string s;
            std::cin >> s;
            if(s == E)  
        }
    }
    return 0;
}