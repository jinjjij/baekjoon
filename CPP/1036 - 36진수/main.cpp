#include <iostream>
#include <string>
#include <vector>

// 36진법 계산
std::string add(std::string a, std::string b){
    int aLen = a.length();
    int bLen = b.length();

    for(int i=0;i<(aLen>bLen?aLen:bLen);i++){
        
    }
}


std::string mult(std::string a, std::string b){

}


std::string shift(std::string a, int pos){

}


int main(){
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);

// input
    int N = 0;
    std::vector<std::string> words(N);
    std::vector<std::string> alphaCount(37);


    for(int i=0;i<N;i++){
        std::cin >> words[i];
        for(int j=0;j<words[i].length();j++){
            
        }
    }

    int K = 0;
    std::cin >> K;

// logic



    
    return 0;
}